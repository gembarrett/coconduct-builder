$('#additionalSpaces label').click(function(){
  // $clickedID = $(this).getElementsByTagName('input')[0].attr('id');
  // this.parentElement.classList.toggle('selected');
  // console.log($clickedID);
  // if ($clickedID == 'additionalSpaceRulesYes') {
  //   additionalSpaces = true;
  // } else {
  //   additionalSpaces = false;
  // }
  // // as long as one of the radios is selected, proceed to next section
  // $('#next').prop('disabled',false);

});
